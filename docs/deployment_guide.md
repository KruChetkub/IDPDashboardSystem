# การนำโปรเจกต์ขึ้น Vercel (Deployment Guide)

ขั้นตอนการนำเว็บไซต์ขึ้นออนไลน์ผ่าน Vercel โดยเชื่อมต่อกับ GitHub ที่เราเพิ่งทำเสร็จไปครับ

## ขั้นตอนการติดตั้ง (Step-by-Step)

1.  **เข้าสู่เว็บไซต์ Vercel**: ไปที่ [https://vercel.com](https://vercel.com)
2.  **เข้าสู่ระบบ (Login)**: แนะนำให้เลือก **"Continue with GitHub"** เพื่อให้ง่ายต่อการดึงโปรเจกต์
3.  **สร้างโปรเจกต์ใหม่**:
    - กดปุ่ม **"Add New..."** -> เลือก **"Project"**
4.  **เลือก Import Git Repository**:
    - ในฝั่งซ้ายมือ คุณจะเห็นรายชื่อ Repository จาก GitHub ของคุณ
    - ค้นหาชื่อ `IDPDashboardSystem`
    - กดปุ่ม **"Import"**
5.  **ตั้งค่าโปรเจกต์ (Configure Project)**:
    - **Framework Preset**: ระบบจะเลือก `Vite` ให้อัตโนมัติ (ถ้าไม่เลือก ให้เลือก Vite)
    - **Root Directory**: **สำคัญมาก!** ให้คลิก `Edit` แล้วเลือกโฟลเดอร์ `IDP-Dashboard-Vite` (เพราะเราสร้างโปรเจกต์ไว้ในโฟลเดอร์ย่อยนี้)
    - ส่วนอื่นๆ ปล่อยเป็นค่า Default ได้เลย
6.  **กด Deploy**: คลิกปุ่ม **"Deploy"** สีดำด้านล่าง
7.  **รอสักครู่**: ระบบจะทำการ Build และเมื่อเสร็จสิ้นจะมีหน้าจอแสดงความยินดีพร้อมรูปหน้าเว็บของเรา

## การตรวจสอบ
เมื่อ Deploy เสร็จแล้ว คุณจะได้ลิงก์ (เช่น `idp-dashboard-system.vercel.app`) สามารถส่งลิงก์นั้นมาให้ผมช่วยเช็ค หรือแชร์ให้คนอื่นใช้งานได้เลยครับ

## การอัปเดตเว็บไซต์ (Manual Deployment)

หากคุณแก้ไขโค้ดเสร็จแล้วและต้องการอัปเดตเว็บไซต์บน Vercel ให้ทำตามขั้นตอนง่ายๆ ดังนี้ผ่าน Terminal (VS Code):

1.  **บันทึกการเปลี่ยนแปลงทั้งหมด**:
    ```bash
    git add .
    ```

2.  **ยืนยันการแก้ไข (Commit)** พร้อมใส่ข้อความอธิบายสิ่งที่ทำ:
    ```bash
    git commit -m "คำอธิบายสิ่งที่แก้ไข เช่น เพิ่ม Dark Mode"
    ```

3.  **ส่งขึ้น Server (Push)**:
    ```bash
    git push origin main
    ```

    *หากเจอปัญหา Push ไม่ผ่าน ให้ลองรันทีละคำสั่งใหม่ หรือใช้ `git push -u origin main`*

> [!TIP]
> เมื่อรันคำสั่ง `git push` เสร็จสิ้น Vercel จะตรวจพบและเริ่ม Deploy โดยอัตโนมัติ คุณไม่ต้องเข้าไปกดอะไรในเว็บ Vercel เลยครับ รอแค่ 1-2 นาที เว็บก็จะอัปเดตเป็นเวอร์ชันล่าสุดทันที!

## วิธีเช็คสถานะการ Deploy (Verification)

ถ้าคุณอยากรู้ว่าโค้ดขึ้นไปบนเว็บจริงหรือยัง ให้ดูได้จาก 2 ที่นี้ครับ:

### 1. ดูใน GitHub
ในหน้า Repository ของคุณ ตรงรายการไฟล์ล่าสุด จะมีจุดสีๆ ปรากฏขึ้น:
- 🟡 **จุดสีเหลือง**: กำลังทำงาน (Building...)
- 🟢 **เครื่องหมายถูกสีเขียว**: เสร็จสมบูรณ์ (Deployed!)
- 🔴 **กากบาทสีแดง**: เกิดข้อผิดพลาด (Failed)

### 2. ดูใน Vercel Dashboard
- เข้าไปที่ [Vercel Dashboard](https://vercel.com/dashboard)
- คลิกที่โปรเจกต์ของคุณ
- ดูที่สถานะ **"Deployment"** ล่าสุด ถ้าขึ้นว่า **"Ready"** แสดงว่าเว็บไซต์อัปเดตแล้วครับ
