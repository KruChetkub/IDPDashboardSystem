# แผนการรักษาความปลอดภัยข้อมูล (Data Security Plan)

## เป้าหมาย
ซ่อน URL ของ Google Sheet CSV ไม่ให้ปรากฏใน Source Code ฝั่ง Frontend เพื่อป้องกันไม่ให้บุคคลภายนอกเข้าถึงข้อมูลต้นทางโดยตรงได้

## วิธีการ (Solution)
เราจะใช้เทคนิค **Backend-for-Frontend (BFF)** โดยสร้าง API กลาง (Serverless Function) บน Vercel เพื่อทำหน้าที่รับส่งข้อมูลแทน

### สถาปัตยกรรมใหม่
1.  **Client (Browser)**: แทนที่จะเรียก Google Sheet โดยตรง จะเปลี่ยนไปเรียก `/api/people` แทน
2.  **Vercel Function (API)**:
    - อ่านค่า URL ลับจาก **Environment Variable**
    - ดึงข้อมูลจาก Google Sheet
    - ส่งข้อมูลกลับไปให้ Client

### สิ่งที่ต้องทำ (Tasks)
1.  **สร้าง Serverless Function**:
    - สร้างไฟล์ `api/people.js` ในโปรเจกต์
    - เขียนโค้ด Node.js เพื่อ Fetch Data
2.  **ปรับปรุง Frontend**:
    - แก้ไข `src/App.jsx` ให้เรียก `/api/people`
3.  **ตั้งค่า Environment Variable**:
    - สร้างไฟล์ `.env` สำหรับ Local Development (และใส่ใน .gitignore)
    - แนะนำวิธีการตั้งค่าใน Vercel Dashboard

## ข้อดี
- 🔒 **ปลอดภัย**: URL จริงจะถูกเก็บใน Server เท่านั้น
- ⚡ **ยืดหยุ่น**: สามารถเพิ่ม Logic การกรองข้อมูลหรือ Cache ที่ฝั่ง Server ได้ในอนาคต
